# :zbib - Bibliography note template for Zettelkasten & zettlr
name: zbib
parent: zettlr

# This is a development version of this match.
# It will develop the form and then transfer it to default.yml.
# See zbibcopy.yml.original.yml for the earlier version that worked before I tried to
# use global definitions for the forms.

# Bibliography Note
matches:
  - triggers: [":zbib", ":bib", ":source"]
    force_clipboard: true
    replace: |
             ---  
             title: {{TAA_form.title}}
             author: 
               name:
               affiliation:
